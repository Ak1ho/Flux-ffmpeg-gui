# 流转 · Flux 使用说明

## 2.0.3 参数滚轮与新下拉框

滚轮现在只用于滚动，不再调整任何参数。鼠标停在整数、小数输入框或收起的下拉框上时，即使控件获得输入焦点，滚轮也不会改变当前值；所在页面仍可滚动。数字可继续直接输入、点击上下按钮，或使用键盘方向键调整。

点击下拉框会打开自绘圆角浮层，沿用深色与紫蓝配色，带展开/收起、悬停和箭头旋转动效。当前参数右侧显示对勾。长列表内滚轮只滚动选项，不改变已选参数。

- 键盘 Space、Enter、F4 或上下方向键可打开收起的下拉框。
- 列表内方向键、Home/End、PageUp/PageDown 用于浏览；Enter/Space 确认选项。
- Esc/F4 取消，保留原参数；Tab 取消并移动到下一表单项。
- 点击外部取消，窗口移动/缩放/隐藏、控件禁用或候选列表更新时安全关闭，不残留浮窗。
- “设置 → 光影与动效”的总开关同时控制新下拉框动效。总开关关闭时仍保留圆角样式，立即展开/收起。

## 2.0.2 路径与移动软件

- 内置资源使用 `@app/` 标记，不写死安装位置。在成品中，`@app/` 对应当前 `Flux.exe` 旁边的 `_internal` 资源目录。
- FFmpeg 默认设置为 `@app/engines`；酷狗 key 为 `@app/engines/kugou_key.xz`；酷我签名文件也使用同样的相对标记。
- 整个软件目录移动、复制或改名后，内置资源自动跟随。旧版设置和未执行任务中可识别的内置绝对路径会自动转换；仍存在的旧副本不再被误用。
- 可浏览选择外部引擎或文件。自定义外部路径保持原值，缺失时明确报错，不擅自回退到内置文件。
- 输入素材、输出目录和本机音乐数据库不属于内置资源。移动软件不等于移动这些文件，文件自行搬走后需要重新选择。
- 设置、任务、预设和日志继续保存在当前用户的 `%LOCALAPPDATA%\MediaWorkbench`，不会随软件文件夹一起搬走；这不改变已有的用户数据兼容策略。

版本 2.0.1 起，媒体工坊更名为「流转 · Flux」，启用紫蓝色斜切播放图标。程序入口为 `Flux.exe`；窗口、侧栏、关于页、播放预览标题、任务报告和 Windows 文件属性使用新名称。原有处理、解密及第二版动效不变。

为兼容旧版，任务、设置、预设、日志和默认输出目录继续使用原 MediaWorkbench 路径，无需迁移；已有自定义设置不改动。新版与旧版共享单实例锁，不建议同时启动。

## 第二版：光影与切页动效

鼠标经过软件窗口客户区时，紫蓝色光晕跟随移动，附近文字会出现柔和辉光。点击任意客户区位置会泛起两圈逐渐消散的水波。原生标题栏、系统对话框和弹出菜单保持系统行为。

点击左侧导航（包含关于页）时，先收起旧页面文字，再让新文字轻弹出现。转场约半秒；面板不整体移动。转场期间内容区暂不接受点击，避免误操作，左侧仍可连续选择，以最后一次选择为准。缩放窗口、最小化、键盘操作或关闭动效会结束转场并显示最终页面。快捷键切页、参数修改、进度与日志刷新不触发文字转场，后台任务不暂停。

在“设置 → 光影与动效”中可分别关闭鼠标光晕、点击水波或导航转场，也可关闭总开关以减少动态效果。光效强度可调为 30–150%，默认 100%。修改即时预览，点击“保存设置”后下次启动沿用。鼠标静止且所有短动画结束后，动效计时器停止，画面不持续闪动。

第二版与第一版使用相同的本机设置和任务目录；建议不要同时运行两版。旧设置会自动补齐动效默认值，不修改原始媒体文件。

## 2.0.4 批量处理修正

添加多个文件或导入文件夹后，新导入且当前可见的素材会自动选中，并保留之前的可见选择。不再默认只处理第一项。点击“开始处理”或“仅加入队列”前，请核对右侧“将处理 N 个素材”；需要只处理部分文件时，手动重新选择即可。

普通批量为每个选中素材创建独立任务；合并视频/音频仍创建一个包含多个输入的任务。搜索或分类隐藏的素材不参与本次入队，混合类型需要先按左侧分类或只选择同类素材。

队列中的操作名称按媒体类型显示，视频任务正确显示“压缩视频”“调整分辨率”“合并视频”，不会与图片、音频名称混淆。本次不改变已有任务的处理参数。

## 1. 开始一次处理

1. 双击成品 `Flux.exe`。
2. 将文件或文件夹拖入窗口，也可以点击“添加”或“文件夹”。
3. 左侧选择视频、音频、图片或音乐解密分类。
4. 在素材表格中选中要处理的文件。Ctrl / Shift 可多选，“全选”选择当前列表所有文件。
5. 在右侧选择操作、输出格式和参数，滚动右侧面板可查看输出目录和同名策略。
6. 点击“开始处理”立即运行，或“仅加入队列”稍后运行。前者也会启动已有等待任务。

未选中任何行时，处理当前筛选列表内与右侧媒体类型匹配的全部文件。混选不同类型会提示重新选择，不会猜测处理方式。

## 2. 批量与合并

一般批量操作为每个文件生成独立输出。合并操作将选中的同类素材合成一个输出。

合并顺序就是素材表格顺序。先选择单个文件，再用预览区的上/下箭头调整位置。视频合并以第一段尺寸为基准，等比缩放并补边，统一帧率及音频采样格式；无声段补静音。合并音频统一为 48 kHz 立体声后再编码。

## 3. 参数含义

- 开始/结束位置以秒为单位，可输入小数；结束为 0 表示结尾。
- 视频 CRF/CQ 越小通常质量越高、文件越大；“压缩”只是降低数据量的工具，无法保证任意源文件都变小。
- 图片质量越大通常越清晰。PNG/BMP/TIFF 等不会显示无意义的有损质量参数。
- 调整尺寸时宽或高设为 0，自动保持比例。两个都指定则按指定宽高缩放，可能改变比例。
- 视频尺寸需为偶数。裁剪宽、高、X、Y 必须落在原始画面范围内。
- 自动编码默认使用软件编码。WebM 使用 VP9/Opus；NVIDIA、Intel、AMD 编码需要相应硬件支持。
- “移除元数据”不代表完成隐私取证级清理；若有严格隐私要求，请检查最终文件信息。
- GIF 建议只截取短段；GIF 开始、结束、宽度、帧率均可配置。
- 水印按图片原像素尺寸叠加到右下角；请预先准备合适大小的水印。
- 字幕支持 SRT/ASS/SSA，烧录后永久进入画面。推荐 UTF-8 编码，字体依赖本机安装情况。

## 4. 音乐解密

勾选权限确认后才能排队。权限确认不会绕过客户端或账号要求。

| 来源 | 接入格式 | 依赖 |
|---|---|---|
| QQ 音乐 | MFLAC / MGG / MMP4 | 客户端运行，原引擎兼容当前客户端 |
| 酷我 | KWM | 客户端运行、匹配的签名信息 |
| 酷狗 | KGM / KGMA / VPR，含原项目双扩展名 | 原项目 kugou_key.xz |
| 酷狗 | KGG | 匹配的本机 KGMusicV3.db 和原引擎资源 |

在设置页可指定酷狗 key、KGG 数据库、酷我 EXE 和签名文件。QQ/酷我的“运行中”只是检测到进程，不是保证版本兼容。

QQ / 酷我调用运行中客户端的解密流程，存在客户端崩溃或重启风险，请先保存客户端中的重要状态。该风险来自原引擎与客户端版本的兼容性，不会通过伪造成功来掩盖。

选择“保留解密后的原始格式”不会主动重编码。选择 MP3/FLAC/WAV/M4A/OGG/OPUS 则先解密，再调用统一 FFmpeg 转码。仅验证通过的音频会发布到输出目录。

如果失败，进入任务队列查看日志；修正客户端或设置后点“重试选中”，再启动队列。不能通过修改文件扩展名获得真正解密。

## 5. 队列、安全与恢复

- 暂停派发：停止启动新任务，已运行任务继续完成，不是进程挂起。
- 取消选中：停止对应独立任务及其 FFmpeg 子进程，删除该任务的临时输出，不改动原文件。
- 重试选中：重试失败、取消、中断任务，沿用处理参数；重新读取引擎/解密资源设置。
- 自动重命名：默认策略，冲突时添加 `(1)` 等编号。
- 跳过同名：已有输出不被替换。当前实现仍可能先完成处理再决定不发布。
- 覆盖已有输出：原子替换已有结果，但拒绝覆盖原始输入路径。
- 清理已结束：清除完成、取消、跳过的历史行，保留失败与中断记录便于排查；不删除媒体结果。
- 正常退出时会询问是否取消运行任务。异常退出后任务显示“已中断”，不会静默自动重启。
- 为避免后台继续处理，工作进程监测 GUI 所属进程，GUI 消失后退出；异常关机可能留下 `.media-任务编号` 临时目录，重试会尝试清理对应任务目录。

任务日志可能包含文件名、绝对路径、参数和解密错误信息。导出报告或提供日志给别人之前请检查隐私内容。

## 6. 预览、预设与位置

- 选中素材可读取 FFprobe 详情；双击视频/音频会打开独立 FFplay 预览窗口，空格暂停、Esc 关闭。
- 图片在对话框预览；加密音频需先解密。不能解码的特殊图片格式可能无法预览。
- “保存”将当前参数保存为预设；预设按媒体类型和操作过滤，不保存输出目录和授权勾选。
- 设置/任务/日志：`%LOCALAPPDATA%\MediaWorkbench`。
- 默认结果：`%USERPROFILE%\Downloads\MediaWorkbench`。
- 成品内置资源：`_internal\engines` 与 `_internal\vendor`。

## 7. 快捷键

| 快捷键 | 功能 |
|---|---|
| Ctrl+O | 添加文件 |
| Ctrl+Enter | 开始当前处理 / 启动队列 |
| Ctrl+1 | 全部素材 |
| Ctrl+2 | 任务队列 |
| Ctrl+, | 设置 |

移动或分享软件时需要整个成品目录。不要单独复制 EXE，不要删除 `_internal`。原始的 FFmpeg 与解密项目目录未被本应用修改。
